Flask==2.3.2
Flask-WTF==1.2.1
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
alembic==1.13.2 # Explicitly added by user
python-docx==1.1.2
spacy==3.7.6
textblob==0.18.0.post0
gunicorn==22.0.0
psycopg2-binary==2.9.9 # For PostgreSQL, optional for SQLite
python-dotenv==1.0.1
# Adding other dependencies that were in the project previously and are still needed
Flask-Session>=0.4 # From previous merge
Flask-Bcrypt>=1.0  # From previous merge
SQLAlchemy>=1.4 # Ensure this is compatible with Flask-SQLAlchemy 3.1.1 (SQLAlchemy 1.4.x to 2.0.x is generally ok)
WTForms>=3.0 # For Flask-WTF
email-validator>=1.0 # For Email validation in WTForms
langdetect
ibm-watson
ibm-cloud-sdk-core
requests
google-generativeai
stripe
pdfplumber
PyPDF2 # Fallback for pdfplumber
textract==1.6.3 # Pinned due to issues with newer versions
# redis==4.6.0 # User mentioned for future Redis session, not adding yet unless specified for current setup
